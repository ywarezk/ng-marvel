import { FelixValidationDirective } from './felix-validation.directive';

describe('FelixValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new FelixValidationDirective();
    expect(directive).toBeTruthy();
  });
});
